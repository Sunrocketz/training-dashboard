# Дорожная карта

Статусы: `todo` · `doing` · `done` · `blocked`

## Now — стабилизация базы

| ID | Задача | Статус | Зачем |
|----|--------|--------|-------|
| N1 | Пакет docs + Cursor rules + AGENTS | done | Снять архитектурный долг «всё в голове» |
| N2 | Закоммитить `apps-script/` в git | done | Backend в том же репо, что и UI |
| N3 | Проверить Netlify ↔ `main` после первых пушей из Cursor | done | Git linked |
| N4 | Смоук-чек: UI грузит актуальный `/exec` | done | schemaVersion 5 на проде (после GAS deploy) |

## Next — качество без большого рефакторинга

| ID | Задача | Статус | Зачем |
|----|--------|--------|-------|
| OC1 | Операционный контроль: фильтр периода, MoM, отсев, легенда, conv weighted | done | Управленческая польза без смены стека |
| OC2 | Deep-link + CSV + заметный updatedAt + полнее карточка тренера | done | Шаринг, выгрузка, прозрачность данных |
| OC3 | Ясность периода + типографическая иерархия UI | done | Убрать путаницу «вся сумма» vs «к июню» |
| OC4 | Ускорение загрузки: GAS cache + local cache | done | Меньше ожидания белого экрана |
| OC5 | «Распалась» в valid + метка/метрики/аналитика (schemaVersion 3) | done | Согласовать счёт групп с листом; сигнал распада |
| OC6 | Составной рейтинг тренеров TQI (конверсии + отсев + стабильность) | done | Топ не только по 1→5 |
| OC7 | TQI v2 гибрид + полный рейтинг + сильные/слабые стороны (schema v4) | done | Справедливый рейтинг и расширенная аналитика |
| OC8 | Outcome только по законченным группам (D ≤ сегодня; schema v5) | done | Не смешивать идущее обучение с фактом выхода |
| X1 | Вынести DEFAULT URL / конфиг UI в одно место + комментарий в Runbook | todo | Меньше рассинхрона при смене deployment |
| X2 | Добавить в JSON поле `schemaVersion` (non-breaking) | done | Вошло в OC1 (`schemaVersion: 2`) |
| X3 | Фича-флаги UI только через данные, не через копипаст формул | todo | Частично: пороги в `metrics` |
| X4 | Чеклист ручной регрессии в Runbook (уже есть каркас) — дополнять по багам | todo | Нет автотестов |

## Later — структурный рост (только по боли)

| ID | Задача | Статус | Триггер |
|----|--------|--------|---------|
| L1 | Разбить `index.html` на CSS/JS модули (или простой bundler) | todo | Файл мешает ревью / конфликтам |
| L2 | Разбить `Код.js` на Collect / Sheet / Api | todo | Правки в GAS начинают ломать несвязанное |
| L3 | Минимальные тесты на агрегации (чистые функции) | todo | Баги в конверсиях повторяются |
| L4 | Auth / ограничение доступа к API | todo | Требование ИБ или утечка URL |
| L5 | Кэш / ускорение `doGet` | done | Script Cache 5м + localStorage SWR (OC4) |

## Не делаем без ADR

- Переезд с Apps Script на отдельный backend
- Хранение данных не в Sheets
- Добавление Netlify Functions «на будущее»
- Смена формул `conv1to5` без нового поля

## Как двигать карту

1. Идея сначала в [Ideas.md](Ideas.md).
2. Приняли в работу → строка здесь + при необходимости ADR.
3. Сделали → `done` + коротко в Decisions / CHANGE note при архитектурном эффекте.
